{
  "name": "jonron",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "python3 -m http.server 8080",
    "test": "vitest",
    "test:run": "vitest run",
    "test:e2e": "playwright test",
    "fetch-lahman": "node scripts/fetch-lahman.js",
    "generate-roster": "node scripts/generate-roster.js"
  },
  "devDependencies": {
    "vitest": "^4.1.7"
  },
  "private": true,
  "dependencies": {
    "gsap": "^3.15.0",
    "playwright": "^1.60.0"
  }
}
